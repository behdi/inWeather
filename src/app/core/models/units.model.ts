export const enum MeasurementSystem {
  METRIC = 'Metric',
  IMPERIAL = 'Imperial',
}

export const enum TemperatureUnits {
  CELSIUS = 'Celsius',
  FAHRENHEIT = 'Fahrenheit',
}

export const enum WindSpeedUnits {
  METERS_PER_SECOND = 'm/s',
  MILES_PER_HOUR = 'm/h',
}
